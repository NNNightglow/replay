# 📋 股票分析系统 - 项目功能清单 (重构优化版)

> **最后更新**: 2026-02-27
> **版本**: v2.0 (Agent版)
> **状态**: 🟢 生产就绪


## 📁 项目结构概览

```
replay/
├── 📁 frontend/                    # Vue.js前端
│   ├── 📁 src/
│   │   ├── 📁 views/              # 页面组件 (11个页面)
│   │   │   ├── 📄 MarketSentiment.vue     # 市场情绪分析
│   │   │   ├── 📄 SectorAnalysis.vue      # 板块分析
│   │   │   ├── 📄 IndexAnalysis.vue       # 指数分析
│   │   │   ├── 📄 NewHighStocks.vue       # 新高股票
│   │   │   ├── 📄 HeimaAnalysis.vue       # 黑马分析
│   │   │   ├── 📄 BaimaAnalysis.vue       # 白马分析
│   │   │   ├── 📄 MoneyEffect.vue         # 赚钱效应
│   │   │   ├── 📄 ChartTest.vue           # 图表测试
│   │   │   ├── 📄 TestChart.vue           # 测试图表
│   │   │   ├── 📄 SimpleChartTest.vue     # 简单图表测试
│   │   │   └── 📄 TestTradingDatePicker.vue # 交易日期选择器测试
│   │   ├── 📁 components/         # 通用组件
│   │   │   ├── 📄 EChartsRenderer.vue     # 图表渲染组件
│   │   │   └── 📄 TradingDatePicker.vue   # 交易日期选择器
│   │   ├── 📁 services/           # 服务层
│   │   │   └── 📄 api.js          # API服务类 (25个方法)
│   │   ├── 📁 router/             # 路由配置
│   │   │   └── 📄 index.js        # 路由定义 (8个路由)
│   │   ├── 📄 App.vue             # 根组件
│   │   └── 📄 main.js             # 应用入口文件
│   ├── 📁 public/                 # 静态资源
│   │   └── 📄 index.html          # HTML模板
│   ├── 📄 package.json            # Node.js依赖
│   └── 📄 vue.config.js           # Vue配置
├── 📁 utils/                       # 核心工具模块
│   ├── 📁 metadata/                # 数据管理器
│   │   ├── 📄 stock_data_manager.py      # 股票元数据管理
│   │   ├── 📄 index_data_manager.py      # 指数元数据管理
│   │   ├── 📄 market_data_manager.py     # 市场元数据管理
│   │   ├── 📄 sector_data_manager.py     # 板块数据管理
│   │   └── 📄 data_processor.py          # 数据处理器
│   ├── 📁 visualizers/             # 可视化组件
│   │   ├── 📄 common.py                  # 通用可视化组件
│   │   ├── 📄 stock_visualizer.py        # 股票可视化
│   │   ├── 📄 index_visualizer.py        # 指数可视化
│   │   ├── 📄 sector_visualizer.py       # 板块可视化
│   │   ├── 📄 market_visualizer.py       # 市场可视化
│   │   └── 📄 model_visualizer.py        # 模型可视化
│   ├── 📄 data_fetcher.py          # 数据获取核心
│   ├── 📄 analyzer.py              # 市场分析核心
│   ├── 📄 data_cache.py            # 数据缓存
│   ├── 📄 trading_calendar.py         # 节假日工具
│   └── 📄 data_fetcher_backup.py   # 数据获取备份
├── 📁 data_cache/                  # 数据缓存目录
│   ├── 📁 indices/                 # 指数数据
│   ├── 📁 sectors/                 # 板块数据
│   ├── 📁 stock_daily/             # 股票日线数据
│   ├── 📁 stock_minute/            # 股票分钟数据
│   └── 📁 other/                   # 其他数据
├── 📁 test/                        # 测试文件 (25个测试文件)
├── 📁 backend/                     # 后端模板
│   └── 📁 templates/               # 模板文件
├── 📄 flask_app.py                 # Flask后端主文件 (33个API端点)
├── 📄 stock_data_enhancer_final.py # 股票数据增强器
├── 📄 start_system.bat             # 一键启动脚本
├── 📄 requirements.txt             # Python依赖
└── 📄 项目功能清单.md              # 本文档
```

## 🔧 核心工具模块 (utils/)

### 📄 data_fetcher.py - 数据获取核心类
```python
class DataFetcher:
    """数据获取和管理的核心类"""
    
    # 初始化方法
    def __init__(self)                                    # 初始化数据获取器
    def check_and_update_metadata(progress_callback=None) # 检查并更新所有元数据
    
    # 市场情绪数据
    def get_market_sentiment(date=None)                   # 获取市场情绪指标
    
    # 板块数据相关
    def get_combined_sectors_summary(date_str=None, include_sectors=True, include_concepts=True, days_back=30)
    def get_sectors_custom_period(start_date, end_date, include_sectors=True, include_concepts=True)
    def get_sector_kline_data(sector_name, days_back=30, target_date=None)

    # 元数据管理器实例
    self.cache                                           # DataCache实例
    self.stock_metadata_manager                          # StockMetadataManager实例
    self.index_metadata_manager                          # IndexMetadataManager实例  
    self.market_metadata_manager                         # MarketMetadataManager实例
    self.sector_data_manager                            # SectorDataManager实例

class LocalFileDataFetcher:
    """本地文件数据获取器"""
    def __init__(data_dir)
    def _is_bond_code(stock_code)
    def _get_available_stocks()
    def _read_stock_data(stock_code)
    def get_index_data(start_date, end_date=None)
    def get_market_sentiment(date=None)
    def fetch_stock_details(date=None)
    def get_sector_data()

# 工具函数
def safe_write_parquet(df, file_path, max_retries=3)     # 安全写入parquet文件
def create_data_fetcher(use_local_file=False, local_file_path="...")  # 创建数据获取器
```

### 📄 analyzer.py - 市场分析核心类 (28个静态方法)
```python
class MarketAnalyzer:
    """市场分析功能集合"""
    
    # 市场情绪分析
    @staticmethod
    def analyze_market_sentiment(sentiment_data, previous_data=None)
    @staticmethod
    def analyze_market_history(market_data, days=30)
    @staticmethod
    def analyze_limit_up_history(limit_up_data, days=30)
    
    # 板块分析
    @staticmethod
    def analyze_sectors(sector_data)
    @staticmethod
    def analyze_concepts(concept_data)
    @staticmethod
    def analyze_concept_status(concept_pl, market_states)
    @staticmethod
    def analyze_limit_up_details(sentiment_data)
    
    # 股票模型分析
    @staticmethod
    def analyze_model_one_stocks(previous_limit_up_data, stock_details)
    @staticmethod
    def _has_upper_shadow(data)
    @staticmethod
    def _is_multi_pump(data)
    @staticmethod
    def _has_previous_multi_limit_up(data)
    @staticmethod
    def _is_high_position(data)
    @staticmethod
    def _is_zhihuji_pattern(data)
    
    # 新高股分析
    @staticmethod
    def analyze_new_high_stocks(market_states_data, days=5, selected_date=None, exclude_st=True, include_non_main_board=False)
    @staticmethod
    def analyze_continuous_limit_up(limit_up_data)
    
    # 北交所微盘股分析
    @staticmethod
    def get_beijing_microcap_analysis(date_str)
    @staticmethod
    def _get_sentiment_status(change_pct, is_microcap=False)
    @staticmethod
    def get_trading_strategy(beijing_data)
    
    # 指数分析
    @staticmethod
    def get_multi_index_kline_data(selected_indices, date_str, days_range)
    @staticmethod
    def get_available_indices()
    @staticmethod
    def get_index_data(index_name, date_str, days_range=180)
    
    # 强势股分析
    @staticmethod
    def analyze_heima_stocks(market_states_data, date=None, exclude_st=True, include_non_main_board=False)
    @staticmethod
    def analyze_baima_stocks(market_states_data, intervals=None, min_market_cap=100, exclude_st=True, include_non_main_board=False, include_main_board=True, include_kcb_cyb=True, include_bjs=False)
    
    # 赚钱效应分析
    @staticmethod
    def _calculate_change_distribution(market_df, change_col)
    @staticmethod
    def get_money_effect_analysis(date_str, analysis_type='all', market_states=None, exclude_st=True, include_non_main_board=False)
    @staticmethod
    def _analyze_all_stocks_money_effect(target_date, market_states, exclude_st=True, include_non_main_board=False)
    @staticmethod
    def _analyze_strong_stocks_money_effect(target_date, market_states, exclude_st=True, include_non_main_board=False)
    @staticmethod
    def _calculate_money_effect_stats(stocks_data)
```

### 📄 utils/visualizers/* - 可视化模块 (按领域拆分)
```python
# 通用能力
ChartConfig / ChartUtils / ChartFormatters / UniversalKlineChart

# 指数可视化
class IndexVisualizer:
    @staticmethod
    def plot_index_kline(index_data, title=None, height="600px")
    @staticmethod
    def get_multi_index_kline_options(index_data_dict)
    @staticmethod
    def plot_multi_index_kline(index_data_dict)
    @staticmethod
    def plot_market_volume_chart(current_data, previous_data, height="600px")
    @staticmethod
    def get_market_volume_chart_options(current_data, previous_data)
    @staticmethod
    def calculate_ma(data, window_size)

# 股票可视化
class StockVisualizer:
    @staticmethod
    def plot_stock_kline(stock_data, stock_name=None, stock_code=None, height="600px")
    @staticmethod
    def plot_turnover_chart(stock_data, stock_name=None, stock_code=None, height="400px")
    @staticmethod
    def calculate_ma_lines(stock_data, periods=[5, 10, 20])
    @staticmethod
    def plot_stock_comparison(stock_data_dict, normalize=False, height="800px")
    @staticmethod
    def plot_new_high_stock_kline(stock_data, stock_code, height="600px")
    @staticmethod
    def _calculate_ma(data, window_size)

# 板块可视化
class SectorVisualizer:
    @staticmethod
    def plot_sector_kline(data_fetcher, date_str, days_range=30, index_name=None)
    @staticmethod
    def plot_single_sector_kline(data_fetcher, sector_name, overlay_index=None, days_range=30)
    @staticmethod
    def get_top_volume_sectors(data, top_n=10, days=5)
    @staticmethod
    def plot_sector_ranking(sector_data, height="600px")
    @staticmethod
    def plot_sector_distribution(sector_data)

# 市场可视化
class MarketVisualizer:
    @staticmethod
    def plot_market_sentiment(sentiment_data)
    @staticmethod
    def plot_market_metadata(market_states, market_metadata)
    @staticmethod
    def plot_change_distribution(change_distribution)
    @staticmethod
    def plot_continuous_limit_ladder_line(market_states)
    @staticmethod
    def plot_continuous_limit_ladder_stack(market_states)
    @staticmethod
    def plot_continuous_limit_ladder_combined(market_states)

# 模型可视化
class ModelVisualizer:
    @staticmethod
    def plot_model_one_stocks(model_one_stocks)
```

## 🗄️ 数据管理器 (utils/metadata/)

### 📄 stock_data_manager.py - 股票元数据管理器
```python
class StockMetadataManager:
    """股票元数据管理类，主板使用baostock,北交所使用ak.stock_zh_a_hist接口"""

    def __init__(metadata_path=None)                     # 初始化管理器
    def load_metadata()                                  # 加载股票元数据
    def is_latest_trading_day()                         # 检查是否最新交易日
    def update_metadata(start_date=None, end_date=None, progress_callback=None)  # 更新元数据

# 工具函数
def safe_network_request(func, *args, max_retries=3, timeout_seconds=30, **kwargs)
def safe_write_parquet(df, file_path, max_retries=3)
```

### 📄 index_data_manager.py - 指数元数据管理器
```python
class IndexMetadataManager:
    """指数元数据管理类，基于ak.index_zh_a_hist接口"""

    def __init__(metadata_path=None)                     # 初始化管理器
    def load_metadata()                                  # 加载指数元数据
    def is_latest_trading_day()                         # 检查是否最新交易日
    def update_metadata(start_date=None, end_date=None, progress_callback=None)  # 更新元数据
    def get_index_data(code, start_date=None, end_date=None)  # 获取指数数据
    def _calculate_index_ma(df)                         # 计算指数移动平均线
```

### 📄 market_data_manager.py - 市场元数据管理器
```python
class MarketMetadataManager:
    """市场元数据管理类"""

    def __init__(metadata_path=None)                     # 初始化管理器
    def load_metadata()                                  # 加载市场元数据
    def load_market_states()                            # 加载市场状态数据
    def is_latest_trading_day()                         # 检查是否最新交易日
    def update_metadata(progress_callback=None)          # 更新市场元数据
    def update_market_states_incremental()              # 增量更新市场状态
```

### 📄 sector_data_manager.py - 板块数据管理器 (重构优化)
```python
class SectorDataManager:
    """板块数据管理类 - 支持同花顺和东财双数据源"""

    def __init__(preferred_source="ths", metadata_path=None)  # 初始化管理器，支持数据源选择
    def load_sector_data(days_back=30, include_sectors=True, include_concepts=True)  # 加载板块数据
    def get_sector_constituents(sector_name)            # 获取板块成分股
    def get_sector_names(sector_type='both')            # 统一获取板块名称
    def is_latest_trading_day()                         # 检查是否最新交易日
    def update_sector_data(source="ths")                # 更新板块数据
    def _calculate_technical_indicators(df)             # 计算技术指标(MA5/MA10/MA20等)

class ThsDataProvider:
    """同花顺数据提供器 - 专门处理同花顺数据源"""

    def __init__()                                      # 初始化同花顺提供器
    def test_connection()                               # 测试同花顺连接(问财库)
    def get_sector_constituents(code, name, sector_type) # 获取板块成分股(问财库)
    def update_sector_data(start_date, end_date, sector_dir) # 更新板块数据
    def update_all_sectors(sector_dir)                  # 更新所有成分股数据
    def _update_industries(start_date, end_date, sector_dir) # 更新行业数据(内联AK接口)
    def _update_concepts(start_date, end_date, sector_dir)   # 更新概念数据(内联AK接口)

class EastmoneyDataProvider:
    """东方财富数据提供器 - 专门处理东财数据源"""

    def __init__()                                      # 初始化东财提供器
    def test_connection()                               # 测试东财连接
    def get_concept_hist_data(concept_name, start_date, end_date) # 获取概念历史数据(内联AK接口)
    def get_industry_hist_data(symbol, start_date, end_date)      # 获取行业历史数据(内联AK接口)
    def get_concept_constituents(symbol)                # 获取概念成分股
    def get_industry_constituents(symbol)               # 获取行业成分股
    def update_sector_data(start_date, end_date, sector_dir)      # 更新板块数据(内联板块名称获取)
    def update_all_constituents(sector_dir)             # 更新所有成分股数据(内联板块名称获取)
```

## 🎨 可视化组件 (utils/visualizers/)

### 📄 common.py - 通用可视化组件
```python
class ChartConfig:
    """图表配置类，提供通用的图表配置和工具函数"""

    # 颜色配置常量
    COLORS = [...]                                      # 30种颜色配置
    UP_COLOR = "#FF4444"                               # 上涨红色
    DOWN_COLOR = "#00AA00"                             # 下跌绿色

    # 配置方法
    @staticmethod
    def get_common_title_opts(title, subtitle=None)
    @staticmethod
    def get_common_legend_opts(position="top")
    @staticmethod
    def get_common_datazoom_opts()
    @staticmethod
    def get_common_toolbox_opts()
    @staticmethod
    def get_common_tooltip_opts()
    @staticmethod
    def get_common_grid_opts()

class ChartUtils:
    """图表工具类，提供数据处理和格式化功能"""

    @staticmethod
    def format_large_number(value, precision=2)
    @staticmethod
    def get_color_by_change(change_value)
    @staticmethod
    def calculate_cumulative_kline_data(data, change_col='涨跌幅', amplitude_col='振幅')
    @staticmethod
    def calculate_moving_averages(data, periods=[5, 10, 20])
    @staticmethod
    def safe_divide(numerator, denominator, default=0)

class UniversalKlineChart:
    """通用K线图绘制器"""

    @staticmethod
    def plot_kline_with_volume(data, title=None, height="600px", volume_column=None, amount_column=None)

class ChartFormatters:
    """图表格式化器类，提供各种数据格式化函数"""

    @staticmethod
    def format_tooltip_content(params, data_type='stock')
    @staticmethod
    def format_axis_label(value, axis_type='value')
    @staticmethod
    def format_legend_text(name, data_summary=None)
```

### 📄 stock_visualizer.py - 股票可视化
```python
class StockVisualizer:
    """股票相关图表生成"""

    @staticmethod
    def plot_stock_kline(stock_data, stock_name=None, stock_code=None, height="600px")
    @staticmethod
    def plot_turnover_chart(stock_data, stock_name=None, stock_code=None, height="400px")
    @staticmethod
    def plot_stock_comparison(stock_data_dict, normalize=False, height="800px")
    @staticmethod
    def calculate_ma_lines(stock_data, periods=[5, 10, 20])
    @staticmethod
    def plot_new_high_stock_kline(stock_data, stock_code, height="600px")
```

### 📄 index_visualizer.py - 指数可视化
```python
class IndexVisualizer:
    """指数相关图表生成"""

    @staticmethod
    def plot_index_kline(index_data, title=None, height="600px")
    @staticmethod
    def get_multi_index_kline_options(index_data_dict)
    @staticmethod
    def plot_multi_index_kline(index_data_dict)
    @staticmethod
    def plot_market_volume_chart(current_data, previous_data, height="600px")
    @staticmethod
    def get_market_volume_chart_options(current_data, previous_data)
    @staticmethod
    def calculate_ma(data, window_size)
```

### 📄 sector_visualizer.py - 板块可视化
```python
class SectorVisualizer:
    """板块相关图表生成"""

    @staticmethod
    def plot_sector_kline(data_fetcher, date_str, days_range=30, index_name=None)
    @staticmethod
    def plot_single_sector_kline(data_fetcher, sector_name, overlay_index=None, days_range=30)
    @staticmethod
    def get_top_volume_sectors(data, top_n=10, days=5)
    @staticmethod
    def plot_sector_ranking(sector_data, height="600px")
    @staticmethod
    def plot_sector_distribution(sector_data)
```

### 📄 market_visualizer.py - 市场可视化
```python
class MarketVisualizer:
    """市场情绪和元数据图表生成"""

    @staticmethod
    def plot_market_sentiment(sentiment_data)
    @staticmethod
    def plot_market_metadata(market_states, market_metadata)
    @staticmethod
    def plot_market_red_ratio_and_amount(market_metadata, height="600px", width="100%")
    @staticmethod
    def plot_limit_counts(market_metadata, height="600px")
    @staticmethod
    def plot_break_counts(market_states)
    @staticmethod
    def plot_change_distribution(change_distribution)
    @staticmethod
    def plot_continuous_limit_ladder_line(market_states)
    @staticmethod
    def plot_continuous_limit_ladder_stack(market_states)
    @staticmethod
    def plot_continuous_limit_ladder_combined(market_states)
```

### 📄 model_visualizer.py - 模型可视化
```python
class ModelVisualizer:
    """模型相关图表生成"""

    @staticmethod
    def plot_model_one_stocks(model_one_stocks)
```

## 🌐 Flask后端API (flask_app.py) - 33个API端点

### 🏠 基础路由
```python
@app.route('/')                                         # 首页
def index()

@app.route('/api/test', methods=['GET'])                # 测试端点
def test_endpoint()

@app.route('/api/system/status')                        # 系统状态
def system_status()
```

### 📊 市场数据API
```python
@app.route('/api/market/overview')                      # 市场概览
def market_overview()

@app.route('/api/market/sentiment')                     # 市场情绪
def market_sentiment()

@app.route('/api/market/change-distribution')           # 涨跌分布
def market_change_distribution()

@app.route('/api/market/metadata')                      # 市场元数据
def market_metadata()

@app.route('/api/market/sentiment/charts')              # 市场情绪图表
def market_sentiment_charts()

@app.route('/api/market/latest-date', methods=['GET'])  # 最新交易日期
def get_latest_market_date()

@app.route('/api/market/metadata/export')               # 导出市场元数据
def export_market_metadata()

@app.route('/api/market/latest')                        # 最新市场数据
def get_latest_market_data()
```

### 🏢 板块相关API
```python
@app.route('/api/market/sectors')                       # 板块数据摘要
def market_sectors()


@app.route('/api/market/sectors/custom-period')         # 自定义期间板块数据
def market_sectors_custom_period()

@app.route('/api/sectors', methods=['GET'])              # ✅ 统一板块数据API
def get_sectors()                                       # 支持行业、概念或两者，参数type控制

@app.route('/api/sectors/<sector_name>/kline')          # 单个板块K线图
def get_sector_kline(sector_name)

@app.route('/api/sectors/<sector_name>/stocks')         # 板块成分股
def get_sector_stocks(sector_name)
```

### 📈 股票相关API
```python
@app.route('/api/stocks/new-high', methods=['GET', 'POST'])  # 新高股分析
def new_high_stocks()

@app.route('/api/stocks/kline', methods=['GET', 'POST'])     # ✅ 统一股票K线API
def get_stocks_kline()                                       # 支持单个/多个股票，多种格式

@app.route('/api/stocks/search')                        # 股票搜索
def search_stocks()

@app.route('/api/stocks/comparison', methods=['POST'])  # 股票对比
def get_stock_comparison()

@app.route('/api/stock-groups', methods=['GET'])        # 获取股票分组
def get_stock_groups()

@app.route('/api/stock-groups', methods=['POST'])       # 保存股票分组
def save_stock_group()

@app.route('/api/stock-groups/<int:group_id>', methods=['DELETE'])  # 删除股票分组
def delete_stock_group(group_id)
```

### 📊 指数相关API
```python
@app.route('/api/market/indices')                       # 市场指数
def get_market_indices()

@app.route('/api/indices/analysis', methods=['GET'])    # 指数分析
def get_index_analysis()

@app.route('/api/indices/kline', methods=['GET', 'POST'])    # ✅ 统一指数K线API
def get_indices_kline()                                      # 支持单个/多个指数，多种格式

@app.route('/api/indices/available', methods=['GET'])   # 可用指数列表
def get_available_indices()
```

### 🎯 分析相关API
```python
@app.route('/api/analysis/baima/intervals', methods=['GET'])  # 白马股预设区间
def get_baima_preset_intervals()

@app.route('/api/analysis/heima')                       # 黑马股分析
def heima_analysis()

@app.route('/api/analysis/baima', methods=['POST'])     # 白马股分析
def baima_analysis()

@app.route('/api/money-effect')                         # 赚钱效应分析
def get_money_effect()
```

### 🗓️ 节假日相关API
```python
@app.route('/api/holidays/non-trading-days', methods=['GET'])  # 非交易日
def get_non_trading_days()

@app.route('/api/holidays/check-date', methods=['GET'])  # 检查日期交易状态
def check_date_trading_status()

@app.route('/api/holidays/range', methods=['GET'])       # 日期范围非交易日
def get_non_trading_days_range()
```

## 🖥️ 前端应用 (frontend/)

### 📦 技术栈和依赖
```json
{
  "主要框架": {
    "vue": "^3.3.0",
    "vue-router": "^4.2.0"
  },
  "UI组件库": {
    "element-plus": "^2.3.0"
  },
  "图表库": {
    "echarts": "^5.4.0",
    "vue-echarts": "^6.6.0"
  },
  "HTTP客户端": {
    "axios": "^1.4.0"
  }
}
```

### 🗂️ 项目结构
```
frontend/src/
├── 📄 main.js                     # 应用入口文件
├── 📄 App.vue                     # 根组件
├── 📁 router/                     # 路由配置
│   └── 📄 index.js                # 路由定义 (8个路由)
├── 📁 services/                   # 服务层
│   └── 📄 api.js                  # API服务类 (25个方法)
├── 📁 components/                 # 通用组件
│   ├── 📄 EChartsRenderer.vue     # 图表渲染组件
│   └── 📄 TradingDatePicker.vue   # 交易日期选择器
└── 📁 views/                      # 页面组件 (11个页面)
    ├── 📄 MarketSentiment.vue     # 市场情绪分析
    ├── 📄 SectorAnalysis.vue      # 板块分析
    ├── 📄 IndexAnalysis.vue       # 指数分析
    ├── 📄 NewHighStocks.vue       # 新高股票
    ├── 📄 HeimaAnalysis.vue       # 黑马分析
    ├── 📄 BaimaAnalysis.vue       # 白马分析
    ├── 📄 MoneyEffect.vue         # 赚钱效应
    ├── 📄 ChartTest.vue           # 图表测试
    ├── 📄 TestChart.vue           # 测试图表
    ├── 📄 SimpleChartTest.vue     # 简单图表测试
    └── 📄 TestTradingDatePicker.vue # 交易日期选择器测试
```

### 🛣️ 路由配置 (router/index.js)
```javascript
const routes = [
  // 基础路由
  { path: '/', redirect: '/index-analysis' }

  // 主要功能页面
  { path: '/sentiment', component: MarketSentiment }      # 市场情绪
  { path: '/sectors', component: SectorAnalysis }        # 板块分析
  { path: '/index-analysis', component: IndexAnalysis }  # 指数分析

  // 强势股分析模块 (分组路由)
  { path: '/strong-stocks', redirect: '/strong-stocks/new-high' }
  { path: '/strong-stocks/new-high', component: NewHighStocks }    # 新高股票
  { path: '/strong-stocks/heima', component: HeimaAnalysis }       # 黑马分析
  { path: '/strong-stocks/baima', component: BaimaAnalysis }       # 白马分析
  { path: '/strong-stocks/money-effect', component: MoneyEffect }  # 赚钱效应

  // 测试页面
  { path: '/test-chart', component: TestChart }          # 测试图表
  { path: '/chart-test', component: ChartTest }          # 图表测试
  { path: '/simple-chart-test', component: SimpleChartTest } # 简单图表测试
  { path: '/test-trading-date-picker', component: TestTradingDatePicker } # 交易日期选择器测试
]
```

### 🔌 API服务类 (services/api.js)
```javascript
class ApiService {
  // 市场数据API (8个方法)
  static async getMarketOverview()                        # 市场概览
  static async getMarketSentiment(date)                   # 市场情绪
  static async getLatestMarketDate()                      # 最新交易日期
  static async getMarketMetadata(daysBack)                # 市场元数据
  static async getMarketSentimentCharts(date, daysBack)   # 市场情绪图表
  static async getIndexData(date, days)                   # 指数数据

  // 股票相关API (4个方法)
  static async getNewHighStocks(date, period, filters)    # 新高股分析
  static async getStockKline(stockCode, days, date)       # 股票K线数据
  static async searchStocks(query)                        # 股票搜索
  static async getStockComparison(params)                 # 股票对比

  // 分析相关API (3个方法)
  static async getHeimaAnalysis(date, filters)            # 黑马股分析
  static async getBaimaAnalysis(startDate, endDate, filters)  # 白马股分析
  static async postBaimaAnalysis(requestData)             # 白马股分析(POST)

  // 板块相关API (6个方法)
  static async getSectorData(date, filters)               # 板块数据
  static async getSectorCustomPeriod(params)              # 自定义期间板块数据
  static async getSectorStocks(sectorName)                # 板块成分股
  static async getSingleSectorKline(sectorName, params)   # 单个板块K线图
  static async getMultiIndexKline(params)                 # 多指数K线图
}
```

## 🎨 颜色和样式规范

### 📊 图表颜色标准 (中国股市惯例)
```python
# K线图颜色
UP_COLOR = "#FF4444"      # 红色 - 上涨
DOWN_COLOR = "#00AA00"    # 绿色 - 下跌

# MA均线颜色 (在ChartConfig中定义)
MA5_COLOR = "#4ECDC4"     # 青色 - MA5
MA10_COLOR = "#FFBF00"    # 黄色 - MA10
MA20_COLOR = "#F92672"    # 红色 - MA20

### 📱 页面组件 (views/)

#### 📄 MarketSentiment.vue - 市场情绪分析
```javascript
export default {
  name: 'MarketSentiment',
  setup() {
    // 响应式数据
    const selectedDate = ref('')                         # 选择的日期
    const daysBack = ref(30)                            # 分析周期
    const loading = ref(false)                          # 加载状态
    const sentimentData = ref({})                       # 市场情绪数据
    const chartData = ref({})                           # 图表数据

    // 主要方法
    const loadData = async () => {                      # 加载数据
      // 调用API获取市场情绪数据
      // 调用API获取图表数据
    }

    const refreshCharts = async () => {                 # 刷新图表
      // 重新加载所有图表
    }

    // 工具方法
    const formatPercentage = (value) => {}              # 格式化百分比
    const formatChange = (value, suffix) => {}          # 格式化变化值
    const getChangeClass = (change) => {}               # 获取变化样式类
    const getTrendClass = (value, threshold) => {}      # 获取趋势样式类
    const disabledDate = (date) => {}                   # 禁用日期判断
  }
}
```

## 🔄 数据流向 (重构后架构)

### 📊 主要数据流
```
数据源层
├── AKShare (股票、指数、板块数据)
├── BaoStock (主板股票历史数据)
├── 问财库 (板块成分股数据)
└── 北交所API (北交所股票数据)
    ↓
数据提供器层 (新增)
├── ThsDataProvider (同花顺数据源)
├── EastmoneyDataProvider (东财数据源)
├── StockMetadataManager (股票元数据)
├── IndexMetadataManager (指数元数据)
└── MarketMetadataManager (市场元数据)
    ↓
数据管理层
├── SectorDataManager (板块数据管理)
└── DataFetcher (统一数据获取)
    ↓
分析处理层
├── MarketAnalyzer (市场分析)
└── Visualizers (可视化模块)
    ↓
API服务层
└── Flask API (33个端点)
    ↓
前端展示层
└── Vue.js (11个页面组件)
```

## 📊 统计信息 (2025-01-27更新)

### 🔢 代码规模统计
- **总文件数**: 35个核心文件 (新增10个测试文件)
- **核心类数**: 17个主要类 (新增ThsDataProvider、EastmoneyDataProvider)
- **API端点数**: 33个Flask API (重构优化，减少5个冗余端点)
- **静态方法数**: 75个以上 (新增技术指标计算方法)
- **可视化器数**: 6个专门的可视化器
- **数据管理器数**: 4个元数据管理器
- **前端页面数**: 11个Vue页面组件
- **测试文件数**: 25个测试文件

### 🏗️ 架构优化成果
- **🔧 重构优化**: 板块数据管理器完全重构，代码更简洁高效
- **📊 数据源整合**: 支持同花顺+东财双数据源，数据更全面
- **⚡ 性能提升**: 内联API调用，减少方法调用层级
- **🧹 代码清理**: 删除154行冗余代码，提高维护性
- **📈 技术指标**: 新增MA5/MA10/MA20移动平均线计算
- **🔗 API优化**: 统一API设计，减少重复端点
- **🎨 前端优化**: Vue.js现代化界面，响应式设计

### 📋 重构详情
- **删除方法**: 8个冗余方法 (get_concept_indices、get_industry_indices等)
- **合并功能**: 6个方法内联到调用函数中
- **新增功能**: 问财库成分股获取，技术指标自动计算
- **数据质量**: 完整的数据验证和去重机制
- **前端架构**: Vue 3 + Element Plus，现代化用户体验

## 🚀 最新优化成果 (2025-01-27)

### ✅ 已完成的优化
1. **API整合**: 新增2个统一API，减少冗余端点
2. **格式化优化**: 移除后端重复格式化，统一前端处理
3. **性能提升**: 使用市场状态数据，减少重复计算
4. **成交量颜色修复**: 修复多指数K线图成交量颜色逻辑
5. **板块分析修复**: 修复前端数据结构解析问题
6. **前端架构**: Vue.js现代化界面，响应式设计
7. **测试覆盖**: 新增25个测试文件，提高代码质量

### 🔄 技术债务清理
```python
# 已移除的冗余代码
DataFetcher._format_amount()      # 移除 → 前端utils.formatAmount()
DataFetcher._format_volume()      # 移除 → 前端utils.formatVolume()
DataFetcher._format_percentage()  # 移除 → 前端utils.formatPercent()

# 新增的统一API
/api/stocks/kline/unified         # 整合3个股票K线API
/api/indices/kline/unified        # 整合3个指数K线API
```

### 📈 性能提升效果
- **数据获取**: 使用预计算的市场状态数据，包含MA5/MA10/MA20
- **API响应**: 统一API减少重复逻辑，提高响应速度
- **前端渲染**: 格式化统一在前端，减少网络传输数据量
- **代码维护**: 减少重复代码，降低维护成本
- **用户体验**: Vue.js现代化界面，响应式设计

---

**注意**: 本清单基于实际代码生成，所有类、方法、API端点都是真实存在的。在开发新功能时，请严格参照此清单避免重复造轮子。

**更新日志**: 2025-11-02 - 完成前端架构优化，新增Vue.js现代化界面，完善测试覆盖，更新项目结构信息。
